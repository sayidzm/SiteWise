# Tech Context

Stack:

```text
HTML5
CSS3
Vanilla JS ES Modules
LocalStorage
Service Worker
Cache Storage
Wake Lock / Vibration / VisualViewport progressive enhancement
```

No React/Next/Vue/Angular. No backend/auth by default.

Run locally:

```bash
python -m http.server 8080
```

Tests:

```bash
node tests/run-all.mjs
```

Service Worker `file://` üzerinde register olmaz; localhost/HTTPS gerekir.
