# Active Context

Current snapshot: **FAZ 11**.

Most recently hardened area: backup/import/export integrity.

Important current constants:

```text
state schemaVersion = 1
backup formatVersion = 1
backup appVersion = 0.11.0
max import = 5 MB
```

Next formal work if roadmap continues:

1. FAZ 12: PWA/offline validation hardening.
2. FAZ 13: real mobile viewport + edge/data integrity final QA.

Do not assume browser visual QA is complete.
